// we run test in node12, so we to set a lower target for mocha

{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "target": "ES2019",
    "lib": [
      // error.cause is used in AssumeRoleProvider
      "ES2022.Error"
    ]
  }
}
